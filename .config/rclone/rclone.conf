# rclone config template - leave client_id/client_secret empty to use
# rclone's built-in Google client (works out of the box).
# Never commit a real token or client_secret to this repo.

[gdrive]
type = drive
scope = drive
