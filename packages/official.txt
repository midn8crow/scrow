# SCROW — Official (pacman) package manifest
# One package per line. Comments and blank lines are ignored.
# This is the SINGLE SOURCE OF TRUTH for official packages.

# Core Hyprland
hyprland
hyprlock
hyprutils
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
xdg-utils
wl-clipboard
cliphist
grim
slurp
satty
awww

# Networking & Bluetooth
networkmanager
network-manager-applet
blueman
nm-connection-editor
gnome-keyring
kdeconnect

# Input
fcitx5
fcitx5-configtool
fcitx5-gtk
fcitx5-qt

# Audio
pipewire
pipewire-audio
pipewire-pulse
wireplumber
pipewire-alsa
pipewire-jack
pavucontrol
alsa-utils

# Status bar (waybar-cava local build deps: only what our configs use - the PKGBUILD does NOT force --auto-features=enabled, meson skips absent module deps)
# JACK provider: ONLY pipewire-jack (bridge JACK clients onto PipeWire). A real
# jackd server (jack2) conflicts with pipewire-jack and the SCROW audio stack is
# PipeWire/WirePlumber (wpctl, pipewire-pulse, pipewire-alsa) — no jackd needed.
cava
playerctl
jq
fmt
gtk-layer-shell
gtkmm3
jsoncpp
libappindicator
libdbusmenu-gtk3
libevdev
libinput
libmpdclient
libnl
libpulse
libsigc++
sndio
libwireplumber
libxkbcommon
chrono-date
spdlog
upower
# Vendored fork waybar (.local/bin/waybar): built with experimental modules
# (gps/belt/ups/cloudproviders/tinysparql/cava/tray) that link extra libraries
# a fresh install would otherwise lack, so the bar silently fails to start.
# These are the runtime libs its ldd pulls in (verified). Keep in sync with the
# vendored binary; do NOT drop or the right-click island bar disappears.
gpsd
libmm-glib
libcloudproviders
tinysparql
libwacom
iniparser
sdl2-compat
glycin
# waybar-cava makedepends
ninja
scdoc
wayland-protocols
glib2-devel
python-setuptools

# Launcher
rofi
rofi-emoji

# Terminal
kitty

# Notifications
mako
libnotify

# Shell
zsh
zsh-autosuggestions
zsh-syntax-highlighting
starship
fzf
zoxide
eza
neovim
git-delta

# Theming
adw-gtk-theme
qt6ct
papirus-icon-theme

# Fonts
ttf-jetbrains-mono-nerd
noto-fonts
noto-fonts-emoji

# File manager
thunar
gvfs
file-roller
ffmpegthumbnailer

# Games / TUI apps
chess-tui

# Utilities
fastfetch
btop
htop
mpv
yazi
brightnessctl
ffmpeg
imagemagick
tree
7zip
unzip
wget
curl
git
xdg-user-dirs
wtype
zenity
libqalculate
tesseract
tesseract-data-eng
loupe
rclone
fuse3
python-pillow
firefox

# makepkg (waybar-cava build) needs the base-devel toolchain on ANY machine
base-devel

# Hyprpm plugin build deps
cmake
meson
gcc
cpio

# WayClick (mechanical keypress sounds) deps
uv
libuv
python

# AUR helper build deps
rust
go

# System
sddm
grub
efibootmgr
# power-profiles-daemon provides powerprofilesctl, which the SCROW menu uses to
# read/display the active power profile ([active]) and switch performance /
# balanced / power-saver. Without it a fresh install has no powerprofilesctl and
# the menu shows no active marker.
power-profiles-daemon

# Security
nftables
fail2ban
clamav
rkhunter
bubblewrap
lynis
